# rag-citation-qa
RAG (Retrieval-Augmented Generation) project for placement interviews — a system that answers questions from a document corpus with citations back to the source, using hybrid retrieval (vector + keyword search) and reranking.
